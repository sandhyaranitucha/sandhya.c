#include <stdio.h>

int main() {
    char str1[20];
    int i;
    gets(str1);
    int count=0;
    while(str1[i]!='\0')
    {
        if(str1[i]=='')
        count++;
        str1[i++];
    }
    printf("no of words%d",count);
    return 0;
}
